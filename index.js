function createRoom() {
    if(document.getElementsByClassName("text_field").value == undefined) {
        alert("You must type in a name first!")
    }
}

function joinRoom() {
    if(document.getElementsByClassName("text_field").value == undefined) {
        alert("You must type in a name first!")
    }
}